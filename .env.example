# Capy Tracking - Environment Variables Configuration
#
# Note: Capy Tracking is a 100% Client-Side Privacy-First Web App.
# It runs entirely in the user's browser using localStorage.
# No backend, external database, or secret API keys are required for production.
#
# Optional configurations (if needed for custom hosting path):
# VITE_BASE_URL="/"
